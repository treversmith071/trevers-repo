name: Selectr
options:
  bundleIdPrefix: com.selectr
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"

targets:
  Selectr:
    type: application
    platform: iOS
    sources:
      - Selectr
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.selectr.app
        SWIFT_VERSION: 5.0
        INFOPLIST_FILE: Selectr/Info.plist
